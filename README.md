# enderkus.com

Personal blog, built with Jekyll using the [Colophon](https://github.com/enderkus/jekyll-theme-colophon) theme.

## Local development

```bash
bundle install
bundle exec jekyll serve
```

## Deployment

Pushes to `main` are built and deployed to GitHub Pages automatically via `.github/workflows/pages.yml`.
